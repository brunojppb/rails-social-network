<% provide(:title, 'Edit') %>

<h1>Edit Profile</h1>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= form_for(@user) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit 'Update profile', class: 'btn btn-primary' %>
    <% end %>
  </div>
</div>
